version: '3'

services:
  workspace:
    tty: true
    build: docker/node
    ports:
      - 80:3000
    volumes:
      - ./:/var/www
    environment:
      - CHOKIDAR_USEPOLLING=true
    command: >
      bash -c "
        npm install
        npm run dev
      "